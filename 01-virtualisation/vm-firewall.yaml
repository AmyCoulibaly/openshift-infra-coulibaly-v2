apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: fedora-pink-mite-53
  namespace: bbamycoulibaly-dev
  labels:
    app: fedora-pink-mite-53
spec:
  dataVolumeTemplates:
    - apiVersion: cdi.kubevirt.io/v1beta1
      kind: DataVolume
      metadata:
        name: fedora-pink-mite-53
      spec:
        sourceRef:
          kind: DataSource
          name: fedora
          namespace: openshift-virtualization-os-images
        storage:
          resources:
            requests:
              storage: 30Gi
  runStrategy: Manual
  template:
    metadata:
      labels:
        kubevirt.io/domain: fedora-pink-mite-53
    spec:
      domain:
        cpu:
          cores: 1
        devices:
          disks:
            - bootOrder: 1
              disk:
                bus: virtio
              name: rootdisk
            - disk:
                bus: virtio
              name: cloudinitdisk
          interfaces:
            - masquerade: {}
              model: virtio
              name: default
        memory:
          guest: 1Gi
      networks:
        - name: default
          pod: {}
      volumes:
        - dataVolume:
            name: fedora-pink-mite-53
          name: rootdisk
        - cloudInitNoCloud:
            userData: |
              #cloud-config
              user: fedora
              password: 0t3k-4xrc-sdc5
              chpasswd: { expire: False }
          name: cloudinitdisk
